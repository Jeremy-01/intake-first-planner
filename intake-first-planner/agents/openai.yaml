interface:
  display_name: "Intake-First Planner"
  short_description: "Reusable intake for planning and solver projects"
  default_prompt: "Use $intake-first-planner to turn this planning or solver request into a reusable, schema-first workflow before implementation."

policy:
  allow_implicit_invocation: true
